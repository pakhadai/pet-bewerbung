import React from 'react';

const Label = ({ children }) => (
  <label className="theme-text-muted block text-xs font-bold uppercase tracking-wider mb-1.5">{children}</label>
);

export default Label;
