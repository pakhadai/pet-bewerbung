# Development: Node + Vite dev server (hot reload)
FROM node:18-alpine
RUN apk add --no-cache wget

WORKDIR /app

COPY package*.json ./
RUN npm install

# Source mounted as volume – no COPY
EXPOSE 3000

CMD ["npm", "run", "dev"]
